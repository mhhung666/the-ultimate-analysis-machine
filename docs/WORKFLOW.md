# 📊 自動化工作流程說明

## 🎯 完整流程圖

```
┌─────────────────────────────────────────────────────────────┐
│ make analyze-daily                                           │
│ (或 make daily = fetch-all + analyze-daily)                 │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. 執行 run_daily_analysis_claude_cli.sh                    │
│    ├─ 讀取市場數據 (global-indices, holdings-prices, news) │
│    ├─ 生成市場分析 Prompt                                   │
│    ├─ 調用 Claude CLI 生成 market-analysis-{date}.md       │
│    ├─ 生成持倉分析 Prompt                                   │
│    └─ 調用 Claude CLI 生成 holdings-analysis-{date}.md     │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. 自動執行 update_github_pages.sh ✨ 新增!                │
│    ├─ 查找最新的 markdown 報告                             │
│    ├─ 使用 Claude CLI 轉換成 HTML                          │
│    ├─ 保持完全相同的格式和樣式                             │
│    ├─ 更新 docs/market.html                            │
│    ├─ 更新 docs/holdings.html                          │
│    └─ 更新 docs/index.html 的日期                      │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. 完成! 現在可以推送到 GitHub                              │
│    git add docs/ reports/markdown/                           │
│    git commit -m "Update reports for {date}"                │
│    git push origin main                                     │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. GitHub Pages 自動部署                                    │
│    1-2 分鐘後可在網站上查看最新報告                        │
└─────────────────────────────────────────────────────────────┘
```

## 📝 詳細步驟

### Step 1: 生成報告 (已存在)

```bash
make analyze-daily
```

這會:
- ✅ 生成 `reports/markdown/market-analysis-2025-12-02.md`
- ✅ 生成 `reports/markdown/holdings-analysis-2025-12-02.md`

### Step 2: 自動轉換 HTML (新增!)

在 Step 1 完成後,**自動執行**:

```bash
../src/daily-analysis-system/scripts/deployment/update_github_pages.sh
```

這會:
- ✅ 使用 Claude CLI 將 markdown 轉換成 HTML
- ✅ **保持與現有頁面完全相同的格式**:
  - 相同的導航欄
  - 相同的深色主題
  - 相同的 TOC (目錄) 功能
  - 相同的 Back to Top 按鈕
  - 所有 emoji 和樣式
- ✅ 更新 `docs/market.html`
- ✅ 更新 `docs/holdings.html`
- ✅ 更新首頁日期

### Step 3: 推送到 GitHub

**方式 1: 一鍵部署** (最簡單!) ✨

```bash
make deploy
```

這會自動執行:
1. `make update-pages` - 更新 HTML
2. `make commit-auto` - 自動提交
3. `make push` - 推送到 GitHub

**方式 2: 分步執行**

```bash
# 提交 (自動訊息)
make commit-auto

# 推送
make push
```

**方式 3: 自訂訊息**

```bash
# 提交 (會提示輸入訊息)
make commit

# 推送
make push
```

### Step 4: 自動部署

GitHub Pages 會在 1-2 分鐘內自動更新網站。

## 🛠️ 可用指令

### 完整工作流程 (推薦)

```bash
# 方案 A: 從頭到尾全自動
make daily     # 爬取 + 分析 + HTML
make deploy    # 提交 + 推送 → 完成!

# 方案 B: 分步執行
make daily          # 生成報告和 HTML
make preview-pages  # 預覽檢查
make deploy         # 一鍵部署
```

### 分析相關

```bash
# 只生成分析報告 + 更新 HTML
make analyze-daily

# 只更新 HTML (從最新 markdown)
make update-pages
```

### Git 操作

```bash
# 一鍵部署 (推薦)
make deploy         # update-pages + commit + push

# 分步操作
make commit-auto    # 自動提交 (生成訊息)
make commit         # 互動式提交 (自訂訊息)
make push           # 推送到 GitHub
```

### 預覽網站

```bash
# 啟動本地伺服器 http://localhost:8000
make preview-pages
```

## 🎨 格式保證

`update_github_pages.sh` 使用 **Claude CLI** 進行轉換,確保:

1. ✅ **完全相同的 HTML 結構**
   - 與現有的 `market.html` 和 `holdings.html` 一致
   - 包含完整的 TOC JavaScript
   - 包含 Back to Top 按鈕

2. ✅ **保留所有樣式**
   - 使用相同的 `styles.css`
   - 深色主題一致
   - 響應式設計

3. ✅ **保留所有內容**
   - 所有 emoji (📊, 🚨, ⚠️, ⭐)
   - 所有表格
   - 所有列表和格式

4. ✅ **智能導航高亮**
   - Market Analysis 頁面自動高亮 "Market Analysis"
   - Holdings Analysis 頁面自動高亮 "Holdings Analysis"

## 🔧 技術細節

### 為什麼使用 Claude CLI?

1. **格式一致性**: Claude 理解現有頁面的結構,能完美複製
2. **智能轉換**: 自動處理 markdown → HTML 的複雜轉換
3. **質量保證**: 比簡單的 markdown 解析器更可靠
4. **保持樣式**: 能夠理解和保留所有樣式類別

### 轉換腳本邏輯

```bash
# update_github_pages.sh 的核心邏輯

1. 查找最新的 markdown 報告
   ├─ reports/markdown/market-analysis-*.md (最新)
   └─ reports/markdown/holdings-analysis-*.md (最新)

2. 使用 Claude CLI 轉換
   ├─ 讀取 markdown 內容
   ├─ 提供格式要求 (與現有 HTML 一致)
   └─ 輸出完整的 HTML

3. 更新文件
   ├─ 覆蓋 docs/market.html
   ├─ 覆蓋 docs/holdings.html
   └─ 更新 docs/index.html 日期
```

## ⚙️ 配置

無需任何配置! 只要:

1. ✅ 已安裝 Claude CLI (`claude login` 完成)
2. ✅ 腳本有執行權限 (已設置)
3. ✅ 在 project root 執行 `make` 指令

## ❓ 常見問題

### Q: 如果沒有 Claude CLI 會怎樣?

A: `update_github_pages.sh` 會偵測並跳過 HTML 更新,只生成 markdown 報告。你可以稍後手動更新:

```bash
# 安裝 Claude CLI 後
make update-pages
```

### Q: 可以手動編輯 HTML 嗎?

A: 可以,但下次自動更新會覆蓋。建議:
- 樣式修改 → 編輯 `docs/styles.css`
- 結構修改 → 修改 `update_github_pages.sh` 的 prompt

### Q: 轉換需要多久?

A: 通常 30-60 秒 (兩個頁面)。Claude CLI 調用速度較快。

### Q: 如何驗證 HTML 正確?

A: 使用預覽:

```bash
make preview-pages
# 打開 http://localhost:8000
```

## 📚 相關文件

- [GITHUB_PAGES_SETUP.md](../GITHUB_PAGES_SETUP.md) - GitHub Pages 完整設置指南
- [scripts/analysis/run_daily_analysis_claude_cli.sh](../src/daily-analysis-system/scripts/analysis/run_daily_analysis_claude_cli.sh) - 主分析腳本
- [scripts/deployment/update_github_pages.sh](../src/daily-analysis-system/scripts/deployment/update_github_pages.sh) - HTML 更新腳本
- [Makefile](../Makefile) - 所有可用指令

## 🎉 總結

現在整個流程完全自動化:

```bash
# 一行指令完成所有工作
make daily

# 結果:
# ✅ Markdown 報告已生成
# ✅ HTML 頁面已更新
# ✅ 格式完全一致
# ✅ 只需 git push 即可發布!
```

**會符合目前頁面的格式嗎?**

👉 **會的!** `update_github_pages.sh` 使用 Claude CLI 並明確要求:
- 與現有 HTML 完全相同的結構
- 使用相同的 `styles.css`
- 包含完整的 TOC 和 JavaScript
- 保留所有 emoji 和樣式
- 保持深色主題

所以您目前滿意的格式會完全保留! ✨
